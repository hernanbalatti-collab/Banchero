# Local: archivo SQLite. Producción: URL de Turso (libsql://<base>.turso.io)
DATABASE_URL="file:./prisma/dev.db"
# Solo para Turso: token de acceso (turso db tokens create <base>)
DATABASE_AUTH_TOKEN=""
# Generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
SESSION_SECRET=""
